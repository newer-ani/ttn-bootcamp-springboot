package com.ttn.Spring_Boot.Assingments.answerforlast2questions;

public class Answer {
    /*
    * Answer 9: The evict() method in Hibernate’s Session interface removes a particular object
    *           from the first-level cache (also called the persistence context).
    * Answer 10: The @Transactional annotation in Spring marks a method or class to run within a database transaction.
                 It has the following responsibilities :
                 Begins a transaction when the method starts.
                 Commits the transaction if the method completes successfully.
                 Rolls back the transaction if a runtime exception (unchecked) is thrown.
    * */
}
